# 策略模式

## 定义一系列的算法, 把它们一个个封装起来, 并且使它们可以相互替换

## 定义一系列的算法, 把他们各自封装成策略类,算法封装在策略类内部的方法里;
## 使他们相互可以替换是相对静态类型语言来说的,因为静态类型有类型检查机制,所以各个策略类要实现同样的方法,当他们的真正类型被隐藏在接口后面时,它们才能相互替换
## JS本身任何对象都可以被替换使用,所以要想他们可以相互替换就需要具有相同的 目标及意图 实际就是函数意图和形参出参